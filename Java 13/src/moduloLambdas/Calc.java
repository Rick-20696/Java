package moduloLambdas;

// FunctionalInterface accept only one abstract method!
// I can have many methods, but one has be abstract

public interface Calc {
	//Abstract Class
	
	//Abstract method
	double execute (double a, double b);
	
}
