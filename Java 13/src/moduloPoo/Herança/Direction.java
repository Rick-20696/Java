package moduloPoo.Herança;

public enum Direction {
	/*
	 * ENUM
	 * used when you have a defined amount of options.
	 * is better than using reference values
	 * EX: 1 to "north", 2 to "south"
	 */

	norte, leste, oeste, sul;
}
